# Function for computing posterior predictions

# Compute the loglikelihood of the given test point for the given dendrogram
pos_predict = function(dend, point) {
  # TODO: Enforce that add_weights and other necessary functions have been
  # called on the dendrogram
  log(1)
}
